# Store and check out text files with LF, whatever core.autocrlf says. Shell scripts with
# CRLF fail inside the Linux containers ("/bin/bash^M: bad interpreter").
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.ico binary
